{
  "name": "cross-stitch-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format": "echo \"Error: no test specified\" && exit 0",
    "lint": "echo \"Error: no test specified\" && exit 0",
    "build": "echo \"Error: no test specified\" && exit 0",
    "typecheck": "echo \"Error: no test specified\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.18.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.12.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}